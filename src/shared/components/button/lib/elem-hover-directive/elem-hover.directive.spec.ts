import { ElemHoverDirective } from './elem-hover.directive';

describe('ElemHoverDirective', () => {
    it('should create an instance', () => {
        const directive = new ElemHoverDirective();

        expect(directive).toBeTruthy();
    });
});
